По джанге:

1. Создать проект с такими эндпоинтами (читай - адресами):

now/  -> Возвращает в браузер строку "Now is: <дата и время текущие>"

config/ -> Возвращает JSON-форматированный словарь:

{ "host": "localhost", "port": 8080, "service": "dummy"}

browse/ -> Возвращает список папок и файлов в папке с проектом


Миграции для данной задачи не обязательны.
